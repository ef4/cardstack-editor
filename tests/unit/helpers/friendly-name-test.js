import { friendlyName } from 'dummy/helpers/friendly-name';
import { module, test } from 'qunit';

module('Unit | Helper | friendly name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = friendlyName([42]);
  assert.ok(result);
});
