import { isComponent } from 'dummy/helpers/is-component';
import { module, test } from 'qunit';

module('Unit | Helper | is component');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isComponent([42]);
  assert.ok(result);
});
