import { assert } from 'dummy/helpers/assert';
import { module, test } from 'qunit';

module('Unit | Helper | assert');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assert([42]);
  assert.ok(result);
});
