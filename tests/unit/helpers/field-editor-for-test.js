import { fieldEditorFor } from 'dummy/helpers/field-editor-for';
import { module, test } from 'qunit';

module('Unit | Helper | field editor for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fieldEditorFor([42]);
  assert.ok(result);
});
