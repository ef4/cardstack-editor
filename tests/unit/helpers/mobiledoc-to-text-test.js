import { mobiledocToText } from 'dummy/helpers/mobiledoc-to-text';
import { module, test } from 'qunit';

module('Unit | Helper | mobiledoc to text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mobiledocToText([42]);
  assert.ok(result);
});
