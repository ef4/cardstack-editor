import { nonEmptyField } from 'dummy/helpers/non-empty-field';
import { module, test } from 'qunit';

module('Unit | Helper | non empty field');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nonEmptyField([42]);
  assert.ok(result);
});
