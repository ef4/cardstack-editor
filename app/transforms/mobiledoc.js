export { default } from './passthrough';

// This transform doesn't need to do anything at the moment, but it
// lets us document explicitly which model fields are mobiledoc
// formatted. The content editor uses that fact to know which fields
// need to be given mobiledoc editors.
